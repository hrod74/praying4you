# utils/

Small, pure helper functions. **Empty in Phase A** (foundation only).

Planned (later phases):

- `format.ts` — date formatting, text truncation, character-count helpers (Phase C+).

Keep helpers pure and framework-agnostic so they are easy to unit test.
