# data/

Seed mock/local data, shaped exactly like the typed models in `src/models/types.ts`.
**Empty in Phase A** (foundation only). Accessed only through `src/services/`, never
imported directly by screens.

Planned (later phases):

- `mockPrayers.ts` — ~8–12 varied, clearly fictional prayer requests (Phase C).
- `mockVerses.ts` — a small curated set of verses (Phase F).

No real names, personal data, secrets, or config — fictional sample content only.
